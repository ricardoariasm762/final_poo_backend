spring.application.name=torneo-system

server.port=${PORT:8080}

# Base de datos
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d81ld5osfn5c73fcncv0-a.oregon-postgres.render.com/torneo_23x2?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:a8aSuhhuBn88OdIvdgPEnBpWDUgV8K0G}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET:clave-super-secreta-de-al-menos-32-caracteres-para-hs256}
jwt.expiration=${JWT_EXPIRATION:86400000}
